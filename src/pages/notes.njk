---
title: "Notes"
description: "Some notes."
extraStyle: ["/style/writing.css"]
---

<a If you'd like to be notified of future posts there is an <a href="feed.xml">RSS (Atom) feed</a> for this page.

<ul>
  {% for post in collections.notes | reverse %}

  <li class="[&>*]:inline py-2">
    <a href="{{ post.url }}">{{ post.data.title }}</a>
    {% if post.data.date %} ({% year post.data.date %}) {% endif %}
  </li>
  {% endfor %}
</ul>
